# 2026-08 迭代日志

## 2026-08-02 — 安全审计 + 认证加固

**背景：** 运行 gstack `/cso` 全量安全审计（Phase 0-14），发现 3 项问题并修复前 2 项（HIGH + MEDIUM）。报告存 `.gstack/security-reports/`（已 gitignore，仅本地）。

### 审计发现
1. **HIGH — JWT 签名密钥明文写进 `_knowledge/` 文档**（未提交状态，但 `_knowledge/` 是 git 跟踪目录，提交即永久泄露）
2. **MEDIUM — SMS_MODE≠production 时，验证码降级分支把 OTP 发给任意客户端**（缺 `isLocalRequest` 门控）
3. **MEDIUM — 4 位验证码 + 猜错不失效 + 无 IP 级限流**（暂缓，未处理）

### 已修复
- **JWT_SECRET 轮换** — 旧值（30 位可读短语，弱熵）换成 `openssl rand -base64 48` 随机强密钥。`.env` 轮换，所有已签发 token 失效，需重新登录
- **文档脱敏** — `_knowledge/troubleshooting/stale-server-process-jwt.md` 两处真实密钥 → 占位符；安全报告 JSON 同步脱敏
- **新增 `.gitleaks.toml`** — 内置规则 + 自定义 JWT_SECRET 规则，.env/报告/锁文件豁免，防密钥再进 git
- **`.gitignore`** — 新增 `.gstack/`（安全报告仅留本地）
- **`server/auth.cjs`** — 降级分支 `_dev_code` 加 `isLocalRequest` 门控，与成功分支一致，远程客户端不再拿到验证码
- **服务端重启** — 确认端口释放后启动，用新密钥运行，health 通过

### SOP 沉淀
- `_knowledge/lessons/jwt-rotation-and-server-restart.md` — 密钥轮换 + 服务端重启完整流程（含误杀 workbuddy MCP 进程的注意事项）

### 待办
- **验证码加固**（审计 Finding 3）：6 位码 + 猜错 3 次失效 + IP 限流，待用户决定
